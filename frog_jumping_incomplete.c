#include <stdio.h>
#include <stdlib.h>

typedef struct node {
  int ;
  
} node;

int main() {
  int*
  int M, N, i, j, R, D;
  scanf("%d %d", &M, &N);
  // Input //
  for (i = 0; i < M; i++) {
    for (j = 0; j < N; j++) {

    }
  }

  for (i = 0; i < M; i++) {
    for (j = 0; j < N; j++) {

    }
  }

  return 0;
}
